# 更新流程

面向日常维护：**改了代码 → 发布到网上**。
（首次部署已经完成，见文末「附录」。）

---

## 0. 当前部署信息

| 项目 | 值 |
| --- | --- |
| 网站地址 | <https://origning.github.io/EcoLink/> |
| 代码仓库（GitHub，会自动发布） | <https://github.com/origning/EcoLink> |
| 代码仓库（Gitee，仅备份源码） | <https://gitee.com/xieyuan123/eco-link> |
| 本地项目目录 | `D:\EcoLinkManagerSystem 2\EcoLinkManagerSystem` |
| 远程名 | `github` = GitHub，`origin` = Gitee |
| 发布方式 | GitHub Actions 自动构建 `dist/` 并发布到 Pages |

> 网址大小写很关键：必须是 `/EcoLink/`（大写 E、大写 L），小写 `ecolink` 会 404。

---

## 1. 日常更新（最常用，只有 3 步）

改完 `src/` 里的代码后，在项目目录执行：

```powershell
cd "D:\EcoLinkManagerSystem 2\EcoLinkManagerSystem"
git add -A
git commit -m "这次改了什么"
git push github main
```

**不需要本地构建**，GitHub 会自动 `npm run build:web` 并发布。

- 进度：打开仓库 → **Actions** 标签页，绿色对勾 = 发布成功
- 时间：通常 1~2 分钟
- 生效：刷新网站即可；手机若还是旧版，多刷新一两次（Service Worker 缓存）

---

## 2. 改完想先在本机看效果

```powershell
npm install     # 首次才需要
npm run dev     # 浏览器打开 http://localhost:5173
```

这是「文件模式」：数据存在电脑的 `data/` 目录（已在 `.gitignore`，不会上传）。

---

## 3. 同时备份到 Gitee（可选）

```powershell
git push origin main
```

Gitee 现在已经**不再提供 Pages**，所以推上去只是保存 / 展示源码，不会发布网站。

想一条命令推两个仓库，可以把两个地址都加进同一个远程的 push 地址（配置一次即可）：

```powershell
git remote set-url --add --push github https://github.com/origning/EcoLink.git
git remote set-url --add --push github https://gitee.com/xieyuan123/eco-link.git
# 之后 git push github main 就会同时推 GitHub 和 Gitee
```

---

## 4. 不用 GitHub 时的「手动发布」备用方案

适合推到 Netlify / Cloudflare / 对象存储等：

```powershell
npm run build:gitee    # 类型检查 + 构建，并把 dist/ 复制到 docs/
```

- **拖拽上传**：打开 <https://app.netlify.com/drop>，把 `dist` 文件夹或 `docs` 拖进去
- **要 zip 包**：

```powershell
Compress-Archive -Path 'dist\*' -DestinationPath 'ecolink-site.zip' -Force
```

（`ecolink-site.zip`、`dist/` 都已在 `.gitignore` 中，不会误传到仓库。）

---

## 5. 构建 / 运行命令一览

| 命令 | 作用 | 产物 |
| --- | --- | --- |
| `npm run dev` | 本地开发（文件模式，带 `/api`） | — |
| `npm run build:web` | 类型检查 + 构建静态网站 | `dist/` |
| `npm run build:gitee` | 上面 + 复制到 `docs/` | `dist/` + `docs/` |
| `npm run static` | 构建并本地起静态服务器预览 | — |
| `npm run app` | 打包并运行桌面版（Electron） | — |
| `npm run dist` | 打安装包（Win/Mac） | `release/` |

---

## 6. 发布后自检清单

- [ ] 打开 <https://origning.github.io/EcoLink/> 首页正常
- [ ] 手机打开同一地址，并「添加到主屏幕」
- [ ] 抽查功能：新增食材 / 录入订单 / 查看汇总 / 导出 Excel
- [ ] Actions 最近一次运行是 success

---

## 7. 常见问题

| 现象 | 原因 / 处理 |
| --- | --- |
| 打开 404 | 地址大小写错误，必须 `https://origning.github.io/EcoLink/` |
| 打开还是旧版 | 浏览器/Service Worker 缓存，多刷新几次或重开页面 |
| push 报 workflow 权限错误 | 用的 PAT 没勾 **`workflow`**；GitHub 登录授权则一般无此问题 |
| Actions 失败 | 去 Actions 看日志，常见是依赖安装（`npm ci`）出错，重跑一次即可 |
| 数据不见了 | 静态托管下数据在**各自浏览器**里，换设备/清缓存前先导出备份 |
| 手机访问慢 | GitHub Pages 在国外，网络波动属正常；可换国内托管（见 DEPLOY.md） |

---

## 8. 数据与备份（重要）

- **线上（GitHub Pages）是「设备模式」**：每台手机的数据各自独立、**互不同步**。
- 换手机、清浏览器缓存、换浏览器前，先到「数据」页**导出备份**（JSON 文件）。
- 电脑上 `npm run dev` 是「文件模式」，数据在项目根目录的 `data/` 里，可整体拷贝。

---

## 附录：首次部署回顾（已完成）

1. 本地 `git init`，提交全部源码，添加远程：
   - `github` → `https://github.com/origning/EcoLink.git`
   - `origin` → `https://gitee.com/xieyuan123/eco-link.git`
2. 推送到 GitHub `main` 分支。
3. 仓库 **Settings → Pages → Source** 设为 **GitHub Actions**。
4. 工作流 `.github/workflows/deploy-pages.yml` 自动构建 `dist/` 并发布。
5. 得到地址 <https://origning.github.io/EcoLink/>。
