# EcoLink - 打包说明

给**其他电脑**用时，打成安装包。对方不用装 Node.js，不用命令行。  
改代码、本机调试请看 [`SYSTEM.md`](SYSTEM.md)。打包流程变了，两份文档一起改。

## 1. 两种用法，不要混

| 谁 | 用什么 | 对方要不要 Node.js |
| --- | --- | --- |
| 开发 / 改功能 | `npm run dev` 或 `Windows-打开采购系统.bat` / `macOS-打开采购系统.command` | 要 |
| 发给同事、门店电脑 | 本说明打出来的 Win / Mac 安装包 | 不要 |
| 手机独立用 | [`SYSTEM.md`](SYSTEM.md) 第 2.7 节：静态网站或 Capacitor | 打安装包的人要 |

Windows / macOS 安装包是 Electron 窗口，里面带着网页和本机存盘服务。手机独立包没有 Node，数据在该手机上。各设备数据默认不通，用「数据」页备份互导。

## 2. 打包前准备（打包这台电脑）

1. 已安装 Node.js 20 或以上（`node -v` 能出版本）。
2. 打开项目根目录，装依赖（换电脑或删过 `node_modules` 时做一次）：

```bash
cd 项目目录
npm install
```

3. **在哪个系统上打，就给哪个系统用：**
   - Mac 安装包 → 必须在 **macOS** 上打
   - Windows 安装包 → 必须在 **Windows** 上打  
   在 Mac 上一般打不出能用的 Windows exe。

4. 第一次打包要下载 Electron，需要能上网。

## 3. 打包命令

在项目根目录执行。

### 3.1 本机先看一眼安装版窗口（可选）

```bash
npm run app
```

会先编译，再弹出「EcoLink」窗口。用来确认界面和存盘正常，还不是安装包。

### 3.2 打 macOS 安装包

在 Mac 上：

```bash
npm run dist:mac
```

成功后得到：

```
release/macOS-EcoLink-安装包-1.0.0.dmg
```

把这个 `.dmg` 拷给对方即可。`release/` 里其他文件夹（如 `mac-arm64`）不用发。

当前这台 Mac 打出来的是 **Apple 芯片 (arm64)** 包。Intel Mac 若打不开，需要在 Intel Mac 上再打一次。

### 3.3 打 Windows 安装包

在 Windows 上：

```bat
npm run dist:win
```

成功后得到：

```
release\Windows-EcoLink-安装包-1.0.0.exe
```

把这个 `.exe` 拷给对方即可。

### 3.4 当前系统各打一份

```bash
npm run dist
```

只生成**当前操作系统**的包，不会同时打出 Win + Mac。

## 4. 打包时实际做了什么

`npm run dist:mac` / `dist:win` 会按顺序：

1. `tsc --noEmit`：类型检查
2. `vite build`：前端打到 `dist/`
3. `esbuild`：把 [`server/standalone.ts`](server/standalone.ts) 打成 [`electron/server.cjs`](electron/server.cjs)
4. `electron-builder`：把 `dist/` + `electron/` 打进安装包，输出到 `release/`

配置在 [`package.json`](package.json) 的 `scripts` 和 `build` 字段。  
窗口入口：[`electron/main.cjs`](electron/main.cjs)。

改打包结果时要动的文件：

- 窗口、数据目录：`electron/main.cjs`
- 安装包里的存盘服务：`server/standalone.ts`、`server/store.ts`
- 包名、版本、安装向导：`package.json` → `version`、`build`

## 5. 对方电脑怎么装

### Windows

1. 双击 `Windows-EcoLink-安装包-1.0.0.exe`
2. 按向导选目录、下一步（可改安装位置）
3. 桌面和开始菜单会出现「EcoLink」
4. 双击即可用，不用装 Node.js

### macOS

1. 双击 `macOS-EcoLink-安装包-1.0.0.dmg`
2. 把「EcoLink」拖进「应用程序」
3. 第一次若提示未签名 / 无法打开：在程序图标上 **右键 → 打开**，再点打开
4. 之后可从启动台打开

对方**不要**拷整个源码项目，也不要运行 `Windows-打开采购系统.bat`。

## 6. 安装之后数据在哪

安装版不写项目目录，写在用户目录，**升级重装一般不会清空订单**。

| 系统 | 数据目录 |
| --- | --- |
| Windows | `%APPDATA%\EcoLink\data\` |
| macOS | `~/Library/Application Support/EcoLink/data/` |

里面仍是：

```
db.json
images/{id}.jpg
images/{id}.thumb.jpg
```

备份或换电脑：把整个 `data` 文件夹拷走，放到新电脑对应目录即可。也可以在应用里打开「数据」页导出 `.json` 备份，到新电脑或手机再导入。电脑、Mac、手机默认各存各的，不会自动同步。

如果以前安装过名为「采购食材订单」的版本，旧数据在 `%APPDATA%\采购食材订单\data\` 或 `~/Library/Application Support/采购食材订单/data/`。把那个 `data` 文件夹拷到上面的 EcoLink 目录即可。

开发模式（`npm start`）仍然写项目里的 `data/`，和安装版不是同一份，不要混用。

## 7. 改版本号再打一版

1. 改 [`package.json`](package.json) 里的 `"version"`，例如 `1.0.0` → `1.0.1`
2. 再执行 `npm run dist:mac` 或 `npm run dist:win`
3. 新文件名会带上新版本号

把新安装包发给对方覆盖安装即可。数据在用户目录，通常不用卸干净再装。

## 8. 常见问题

**Mac 上执行 `dist:win` 失败或 exe 不能用**  
到 Windows 电脑上打。不要用交叉编译凑合。

**Mac 提示开发者无法验证**  
没有苹果开发者签名。让对方右键 → 打开。若仍拦，系统设置 → 隐私与安全性 → 仍要打开。

**打包很慢 / 在下载 Electron**  
第一次会拉运行时，需要网络。失败后重跑同一条命令即可。

**窗口是空的或接口失败**  
先 `npm run app` 看本机安装版窗口。确认 `dist/` 和 `electron/server.cjs` 是刚编出来的。

**对方装完没有以前的订单**  
安装版读的是用户目录，不是你开发时项目里的 `data/`。把开发机或旧电脑的 `data` 拷到第 6 节的路径。

**release 要不要提交 git**  
不要。安装包很大，`.gitignore` 已忽略 `release/` 和 `electron/server.cjs`。只把打好的 dmg/exe 另外发给使用的人。

## 9. 命令速查

```bash
npm install          # 打包机装依赖（一次）
npm run app          # 预览安装版窗口
npm run dist:mac     # 打 Mac 的 dmg
npm run dist:win     # 打 Windows 的 exe（仅 Windows）
npm run dist         # 打当前系统的包
npm run static       # 静态网站，设备本地存盘（可给手机独立用）
npm run cap:android  # 同步网页并打开 Android Studio
npm run cap:ios      # 同步网页并打开 Xcode
```
